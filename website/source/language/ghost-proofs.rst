Ghost and proofs
================

- ``ghost { }`` — stripped at compile time
- ``contract_assert(e)`` — VC at this point
- ``reveal_with_fuel(f, n)`` — in ghost blocks only
- ``spec`` / ``proof`` — not compiled